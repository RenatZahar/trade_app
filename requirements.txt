# Runtime dependencies for the project.
# Recommended flow on Windows 11 + VSCode terminal:
#   py -3.11 -m venv .venv
#   .\.venv\Scripts\Activate.ps1
#   python -m pip install --upgrade pip
#   pip install -r requirements.txt
#
# How to refresh this file automatically later:
# 1) From a CLEAN project venv after successful run/tests:
#      pip freeze > requirements.lock.txt
#    (full pin, reproducible but verbose)
# 2) For a minimal import-based draft:
#      pip install pipreqs
#      pipreqs . --force
#    (can miss optional/runtime-only deps, so review manually)

python-dotenv
pandas
numpy
scipy
scikit-learn
joblib
dask[distributed]
flask
plotly
requests
redis
psutil
aiosqlite
python-bitcoinrpc
colorama
pyarrow
